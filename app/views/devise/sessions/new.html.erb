<%= render "devise/shared/login_background" %>

<%= render "devise/shared/form_template",
  form_title: "Log in",
  form_url: session_path(resource_name),
  form_html: {},
  fields: [
    { name: :email, type: :email, options: { autofocus: true, autocomplete: "email" } },
    { name: :password, type: :password, options: { autocomplete: "current-password" } }
  ],
  submit_text: "Log in",
  show_remember_me: true,
  show_links: true,
  show_errors: false %>

